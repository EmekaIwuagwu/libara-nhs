<aside class="bg-white shadow-lg w-64 min-h-screen fixed left-0 top-0 z-10">
    <div class="p-6 border-b">
        <h2 class="text-2xl font-bold text-gradient">LibaraNHS</h2>
    </div>

    <nav class="p-4">
        <ul class="space-y-2">
            <li>
                <a href="/dashboard" class="flex items-center px-4 py-3 rounded-lg hover:bg-primary/10 text-slate-600 hover:text-primary transition <%= page === 'dashboard' ? 'bg-primary/10 text-primary font-semibold' : '' %>">
                    <span class="mr-3">🏠</span>
                    Dashboard
                </a>
            </li>
            <li>
                <a href="/dashboard/resume" class="flex items-center px-4 py-3 rounded-lg hover:bg-primary/10 text-slate-600 hover:text-primary transition <%= page === 'resume' ? 'bg-primary/10 text-primary font-semibold' : '' %>">
                    <span class="mr-3">📄</span>
                    Resume
                </a>
            </li>
            <li>
                <a href="/dashboard/config/create" class="flex items-center px-4 py-3 rounded-lg hover:bg-primary/10 text-slate-600 hover:text-primary transition <%= page === 'config' ? 'bg-primary/10 text-primary font-semibold' : '' %>">
                    <span class="mr-3">⚙️</span>
                    Application Config
                </a>
            </li>
            <li>
                <a href="/dashboard/config/saved" class="flex items-center px-4 py-3 rounded-lg hover:bg-primary/10 text-slate-600 hover:text-primary transition <%= page === 'saved-config' ? 'bg-primary/10 text-primary font-semibold' : '' %>">
                    <span class="mr-3">💾</span>
                    Saved Configuration
                </a>
            </li>
            <li>
                <a href="/dashboard/automation" class="flex items-center px-4 py-3 rounded-lg hover:bg-primary/10 text-slate-600 hover:text-primary transition <%= page === 'automation' ? 'bg-primary/10 text-primary font-semibold' : '' %>">
                    <span class="mr-3">🤖</span>
                    Automation
                </a>
            </li>
            <li>
                <a href="/dashboard/settings" class="flex items-center px-4 py-3 rounded-lg hover:bg-primary/10 text-slate-600 hover:text-primary transition <%= page === 'settings' ? 'bg-primary/10 text-primary font-semibold' : '' %>">
                    <span class="mr-3">🔧</span>
                    Settings
                </a>
            </li>
        </ul>
    </nav>

    <div class="absolute bottom-0 left-0 right-0 p-4 border-t">
        <div class="flex items-center mb-4">
            <div class="w-10 h-10 rounded-full bg-primary text-white flex items-center justify-center font-bold">
                <%= typeof user !== 'undefined' ? user.first_name.charAt(0) : 'U' %>
            </div>
            <div class="ml-3">
                <p class="font-semibold text-sm"><%= typeof user !== 'undefined' ? user.first_name + ' ' + user.last_name : 'User' %></p>
                <p class="text-xs text-slate-500"><%= typeof user !== 'undefined' && user.plan ? user.plan.charAt(0).toUpperCase() + user.plan.slice(1) : 'Free' %> Plan</p>
            </div>
        </div>
        <a href="/logout" class="btn btn-secondary w-full text-sm">Logout</a>
    </div>
</aside>
